<div class="text-center py-12 text-walnut">
  <p class="text-lg font-heading"><%= message %></p>
  <% if user_signed_in? %>
    <p class="mt-2"><%= link_to "Add one!", new_review_wizard_path, class: "text-clay hover:underline" %></p>
  <% end %>
</div>
